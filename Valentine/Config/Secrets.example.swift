// Secrets setup (safe for GitHub)
// ---------------------------------
// 1. Duplicate THIS file in the same folder and name the copy `Secrets.swift`
// 2. Uncomment the struct below in that copy and paste your Last.fm credentials
// 3. Rebuild — then use Settings → Integrations to connect Last.fm
//
// `Secrets.swift` is in .gitignore and must NEVER be committed to GitHub.
// Only this template (empty placeholders) belongs in the repository.
//
// Last.fm keys: https://www.last.fm/api/account/create
// ListenBrainz: paste your user token in Settings → Integrations (not stored here).

// import Foundation
//
// struct Secrets {
//     static let lastFMApiKey = ""
//     static let lastFMSharedSecret = ""
// }
